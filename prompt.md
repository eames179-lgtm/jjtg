Three.js와 HTML/CSS DOM을 활용하여 모바일에 최적화된 웹게임을 Vibe Coding으로 개발할 거야. 다음 요구사항에 맞춰 전체 프로젝트 구조와 코드를 작성해 줘.

0. 프로젝트 개요

- 모바일 웹 브라우저 환경에 최적화된 성능과 UI 반응성을 확보할 것.

- 3D 씬(Scene) 렌더링은 Three.js 캔버스에서 처리하고, 모든 UI(버튼, 팝업, 텍스트)는 HTML/CSS DOM 오버레이로 분리하여 구현할 것.

1. 로딩 및 언어 선택 화면

- 초기 로딩 바 상단에 한국어(Korean)와 영어(English)를 선택할 수 있는 버튼을 구현해 줘.
- 에셋 로딩이 100% 완료되더라도, 유저가 언어를 선택하기 전까지는 3D 씬이 렌더링되거나 게임이 시작되지 않아야 해.
- 선택된 언어 상태는 전역 변수로 관리하여 게임 내 모든 UI 텍스트에 즉각 반영되도록 해.

2. 바다(Ocean) 및 항적(Wake) 구현

- Y축 기준 0 높이에 바다(Ocean) 역할을 할 PlaneGeometry를 생성해 줘.
- 모바일 최적화를 위해 복잡한 쉐이더 대신 waternormals.jpg를 활용해 물결을 표현해 줘.(Threejs 공식 example 참고)
- Player(배)가 이동할 때 배의 후방에 Wake(항적) 텍스처나 파티클을 발생시켜 자연스러운 항해 이펙트를 연출해 줘.

3. 카메라 컨트롤 (모바일 최적화)

- 모바일 환경에 맞춰 OrbitControls(또는 MapControls)를 수정하여, 한 손가락 스와이프로 시점을 회전하고 두 손가락으로 줌인/줌아웃이 되도록 설정해 줘.
- 카메라는 maxPolarAngle을 제한하여 절대 수면(Y=0) 밑으로 내려가지 않도록 차단해 줘.

4. 맵, 플레이어, 그리고 조향(Helm) UI 조작 로직

- map.glb 파일과 player.glb 파일을 로드해 줘. player가 항해하며 map 내의 trigger# (#은 1~5) 박스와 충돌을 감지(Raycaster 또는 BoundingBox)하는 로직을 작성해 줘.
- 조작 UI: helm.png 이미지를 화면 하단 중앙에 배치해 줘.
- 조향 로직: helm 이미지를 터치 & 드래그하여 회전시키면 player도 회전해야 해. 단, 중앙 기준 3도의 Deadzone(데드존)을 설정하여 미세한 떨림을 방지하고, UI가 2.5도 회전할 때 배 객체는 1도만 회전하도록 감도를 설정해 줘.
- 이동 제어: 배는 '항해'와 '정지' 상태만 존재해. helm UI의 정중앙 동그란 버튼을 누르면 토글 돼야 해.
- 현재 항해 중일 땐 버튼에 "정지"(영어 선택 시 "STOP") 텍스트가 표시되고, 정지 중일 땐 "항해"(영어 선택 시 "SAIL") 텍스트가 표시되게 해 줘.
- 배가 이동할 때는 뒤에서 바라보는 isometric 느낌의 3인칭 뷰로 설정해주고 카메라를 고정해줘.(자연스러운 흔들림이나 회전 지연은 허용)
- 참고로 player.glb 모델은 Blender에서 -Y축을 정면으로 보고 제작했어. Three.js 환경에서 배가 앞으로 이동하거나 조향각에 맞춰 회전할 때, 이 정면 방향이 정확히 이동 방향을 향하도록 벡터(Forward Vector)를 맞춰서 로직을 짜줘.

5. 팝업 시스템 (Trigger 1, 2, 4, 5)

- trigger 박스에 닿으면 아래에서 위로 슬라이드 되며 올라오는 반투명 검정색 전체화면 HTML 팝업을 띄워 줘. 팝업이 뜨면 배는 자동으로 '정지' 상태가 돼야 해.
- 팝업 내부는 스크롤이 가능해야 하며, 맨 하단에 '닫기' 버튼을 만들어 줘.
- 향후 내가 직접 유지보수할 수 있도록, 팝업 DOM 내에 동영상 삽입, 사진 삽입, 텍스트 삽입을 위한 HTML 태그 구조를 미리 짜두고, 한글/영문 분기 처리에 대한 상세한 주석을 달아 줘.
- 샘플으로 Trigger1으로 생성되는 팝업창에는 sample1.webp를 삽입하고 sample_vid.mp4 그리고 텍스트'이것은 샘플입니다' 를 넣어줘

6. 특수 이벤트: 대함미사일 방어 (Trigger 3)

- trigger3에 닿았을 때는 즉시 팝업이 뜨면 안 돼.
- 충돌 감지 즉시 배는 정지하며, alarm.mp3 사운드가 반복 재생되게 해 줘.
- Player 객체 상단 3D 공간(또는 추적하는 UI)에 '⚠️대함미사일이 접근하고 있습니다.'라는 텍스트가 긴장감을 주는 빨간색 Glow 효과와 함께 깜빡이게 해 줘.
- 동시에 하단 helm UI 바로 위에 '대공미사일 발사'라는 긴급 버튼(깜빡이는 이펙트 포함)을 생성해 줘.
- 유저가 이 '대공미사일 발사' 버튼을 눌러야만 비로소 trigger3에 해당하는 팝업창이 올라오고 알람이 꺼지도록 상태 관리를 해 줘.

7. 아이템 획득 로직

- 씬 내에 이름에 'item'이 포함된 메쉬들이 있어. 이들은 분당 8회전(8 RPM)의 속도로 Y축 회전을 계속하고, 가벼운 광원이나 반짝이는 VFX를 반복 재생하여 유저가 직관적으로 목표물임을 알게 해 줘.
- trigger#는 item#과 쌍으로 존재해. 배가 trigger에 접촉하는 순간 쌍으로 묶인 해당 item 메쉬도 씬에서 부드럽게 사라지게(Scale 0 또는 Fade out) 해 줘.
- 아이템 획득 순간, player 주변에 획득 성공을 알리는 짧은 파티클/VFX 이펙트를 재생해 줘.
- 획득한 아이템 수를 추적하는 `item_count` 전역 변수를 관리해 줘.

8. 연구개발 진행률 UI 및 최종 클리어 팝업

- 화면 상단 중앙에 '연구개발 진행률 ##%' 텍스트 UI를 고정해 줘.
- 아이템을 1개 획득할 때마다 20%씩 상승하도록 설정해 줘.
- 진행률이 100% (아이템 5개 모두 획득)를 달성하면 축하 팝업(슬라이드 업 방식)을 띄워 줘.
- 축하 팝업 안에는 클리어 메시지와 함께 특정 Google Form 링크로 이동할 수 있는 버튼(<a href="..." target="_blank">)을 삽입해 줘.
